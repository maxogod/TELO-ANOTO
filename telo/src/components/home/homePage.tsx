import React from 'react';
import PropTypes from 'prop-types';

const homePage = props => {
    return (
        <div>
            
        </div>
    );
};

homePage.propTypes = {
    
};

export default homePage;